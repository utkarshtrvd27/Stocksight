rootLogger.level=error
rootLogger.appenderRefs=stdout
rootLogger.appenderRef.stdout.ref=STDOUT

appender.stdout.type=Console
appender.stdout.name=STDOUT
appender.stdout.layout.type=PatternLayout
appender.stdout.layout.pattern=%d{yy/MM/dd HH:mm:ss} %p %c{1}: %m%n

# Specifically targets the Python boot process bridge
logger.pyspark.name=org.apache.spark.api.python.PythonGatewayServer
logger.pyspark.level=error